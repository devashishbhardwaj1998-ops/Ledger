# Dependencies
node_modules/

# Build output
dist/
dist-ssr/

# Env files (never commit secrets)
.env
.env.local
.env.*.local

# Editor/OS cruft
.vscode/
.DS_Store
*.log
npm-debug.log*

# Sample/uploaded time sheet & planning sheet data (contains real employee names/PII).
# This app runs entirely client-side against user-supplied files - they should never
# end up committed to the repo.
*.csv
*.xlsx
*.xls

# Vite
.vite/
